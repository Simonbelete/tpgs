import 'package:flutter/material.dart';

class NutrationContentButton extends StatelessWidget {
  const NutrationContentButton({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(),
    );
  }
}
